{
  "name": "aegis-link-backend",
  "version": "1.0.0",
  "description": "Aegis-Link Backend API - Zero-Knowledge Consent Bridge",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "psql -U postgres -d aegis_link_db -f database.sql",
    "db:reset": "psql -U postgres -c 'DROP DATABASE IF EXISTS aegis_link_db' && psql -U postgres -c 'CREATE DATABASE aegis_link_db' && npm run db:setup"
  },
  "keywords": [
    "zero-knowledge",
    "blockchain",
    "identity",
    "privacy",
    "verification"
  ],
  "author": "TEAM AEGIS",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "pg": "^8.11.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}